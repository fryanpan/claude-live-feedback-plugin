{
  "$comment": "MCP server registration. Runs the published npm package via npx so this plugin works for users who haven't cloned the monorepo. For local development, `cd packages/mcp && npm link` makes npx resolve to the local workspace. Port is auto-discovered from ~/.claude/live-feedback/server.json (written by scripts/serve.ts on startup); set FEEDBACK_BASE_URL to override.",
  "mcpServers": {
    "live-feedback": {
      "command": "npx",
      "args": ["-y", "@fryanpan/live-feedback-mcp@latest"],
      "env": {
        "FEEDBACK_AUTHOR": "agent"
      }
    }
  }
}
