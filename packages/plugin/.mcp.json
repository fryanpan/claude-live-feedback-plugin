{
  "$comment": "MCP server registration. Runs the published npm package via npx so this plugin works for users who haven't cloned the monorepo. For local development before publishing (or after changes), `cd packages/mcp && npm link` makes npx resolve to the local workspace — same command, no config swap.",
  "mcpServers": {
    "live-feedback": {
      "command": "npx",
      "args": ["-y", "@fryanpan/live-feedback-mcp@latest"],
      "env": {
        "FEEDBACK_BASE_URL": "http://localhost:8787",
        "FEEDBACK_AUTHOR": "agent"
      }
    }
  }
}
