{
  "$comment": "MCP server registration. Today the server lives alongside the plugin in the monorepo (`../server/src/mcp.ts`). That path resolves correctly when the marketplace is added as a local path (the plugin dir stays inside the repo). Remote / self-contained install is a TODO — see packages/plugin/README.md Roadmap.",
  "mcpServers": {
    "live-feedback": {
      "command": "bun",
      "args": ["run", "${CLAUDE_PLUGIN_ROOT}/../server/src/mcp.ts"],
      "env": {
        "FEEDBACK_BASE_URL": "http://localhost:8787",
        "FEEDBACK_AUTHOR": "agent"
      }
    }
  }
}
